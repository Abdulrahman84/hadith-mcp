{
  "name": "@hadith-mcp/data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Hadith data import, validation, and SQLite build package.",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "audit:hadith-api": "npm run build && node dist/audit-hadith-api.js --out-dir ../../data/generated",
    "audit:maktaba-grades-backup": "npm run build && node dist/audit-maktaba-grades-backup.js --out-dir ../../data/generated",
    "audit:meeatif-hadith-datasets": "npm run build && node dist/audit-meeatif-hadith-datasets.js --out-dir ../../data/generated",
    "audit:open-hadith-data": "npm run build && node dist/audit-open-hadith-data.js --out-dir ../../data/generated",
    "build:fixture-sqlite": "npm run build && node dist/build-fixture-sqlite.js",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "csv-parse": "^7.0.0"
  }
}
