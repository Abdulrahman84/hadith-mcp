# Hadith MCP Docs Site

Next.js and Tailwind CSS documentation site for Hadith MCP.

```bash
npm run docs:dev
npm run docs:build
```

The app uses static export mode, so production output is written to `apps/docs/out`.

Localized routes:

- `/` Arabic default
- `/ar` Arabic alias
- `/en` English
